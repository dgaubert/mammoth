div#summaries
  - each summary in summaries
    article
      div.row-fluid
        div.page-header
          a(href='/blog/#{summary.slug}')
            h3.title #{summary.title}
          small Por 
            strong #{summary.author}
          small
            = (moment(summary.created).format('[ el] DD/MM/YYYY'))
      div.row-fluid
        p!= summary.abstract
      div#labels.row-fluid
        a.label.label-important(href='/blog/#{summary.slug}') Leer m&aacutes
        a.label.label-success(href='/blog/category/#{summary.category}') #{summary.category}
        - each tag in summary.tags
          a.label.label-warning(href='/blog/tag/#{tag}') #{tag}
        small [#{summary.commentCounter} Comentarios ]
