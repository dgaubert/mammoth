div.summaries
  - each summary in summaries
    div.summary
      hgroup.summary-title
        a(href='/blog/post/#{summary.slug}')
          h2.title #{summary.title}
        h3.author Por #{summary.author}
        h3.created el #{summary.created.getDate()}/#{summary.created.getMonth()+1}/#{summary.created.getFullYear()}
      div.summary-abstract 
        p #{summary.abstract}
      div.summary-action
        a.link(href='/blog/post/#{summary.slug}') Leer m&aacutes
        a.category #{summary.category}
        - each tag in summary.tags
          a.tag #{tag}
        h3.comments [ #{summary.commentsCount} Comentarios ]